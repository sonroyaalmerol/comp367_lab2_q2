pipeline {
  agent any
  stages {
    stage('Display JENKINS_URL and BUILD_ID') {
      steps {
        echo "JENKINS_URL: ${env.JENKINS_URL}"
        echo "BUILD_ID: ${env.BUILD_ID}"
      }
    }
  }
}
